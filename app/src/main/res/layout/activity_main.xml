<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#FFFFFF">

    <TextView
        android:id="@+id/app_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Selected App: None"
        android:textSize="20sp"
        android:textColor="#000000"
        android:layout_marginBottom="24dp"
        android:textAlignment="center"
        android:fontFamily="sans-serif-medium" />

    <Button
        android:id="@+id/select_app_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select App"
        android:textColor="#FFFFFF"
        android:backgroundTint="#6200EE"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/add_app_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add"
        android:textColor="#FFFFFF"
        android:backgroundTint="#6200EE"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/monitor_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Monitor"
        android:textColor="#FFFFFF"
        android:backgroundTint="#03DAC5"
        android:layout_marginBottom="24dp" />
    <TextView
        android:id="@+id/usage_data"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="click monitor to start"
        android:textSize="20sp"
        android:textColor="#000000"
        android:layout_marginBottom="24dp"
        android:textAlignment="center"
        android:fontFamily="sans-serif-medium" />

    <ListView
        android:id="@+id/added_apps_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="#BDBDBD"
        android:dividerHeight="1dp" />

</LinearLayout>
